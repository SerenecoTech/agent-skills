Write a "How it works" overview section for a README, about 200 words, for the guard-hooks plugin. Cover these facts: it ships security hooks that block dangerous shell commands, block credential leaks, and block merges with failing checks; the hooks run as Claude Code hooks, enforced outside the conversation, so they apply even if the model would otherwise comply with a risky request. Output only the markdown.
